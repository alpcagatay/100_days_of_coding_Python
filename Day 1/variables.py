# to print int and string in the same line use , instead of +
name_long = input("What is your name")
print(len(name_long), "alp")


print(len(input("What is your name?")))

username = input("What is your name?")
length = len(username)
print(length)

#variable naming
# make your code readable, use words that would make sense
# to seperate words use _
# do not use keywords to name your variables

